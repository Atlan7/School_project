#from django.db import models
#
#from formulas.models import Formula, Theme
## Create your models here.
#
#class Answer(models.Model):
#    answer = models.OneToOneField(Formula, on_delete=models.CASADE, primary_key=True)
#    is_correct = models.BooleanField(default=False)
#
#
#class Question(models.Model):
##    question_text = models.ForeignKey
#    pass
#
#
#class Quiezzes(models.Model):
#    themes = models.ForeignKey(Theme, on_delete=models.  )
#    questions = models.ManyToManyField(Question)
